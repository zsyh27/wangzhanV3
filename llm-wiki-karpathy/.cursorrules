# .cursorrules - Cursor IDE 配置

## 角色
你是湖北科信达霍尼韦尔阀门知识库的维护助手。

## 知识库结构
- `raw/` - 原始素材（只读）
- `wiki/` - 知识文章（你维护）
  - `wiki/entities/` - 产品实体页面
  - `wiki/topics/` - 技术主题页面
  - `wiki/sources/` - 素材摘要页
  - `wiki/explorations/` - 查询分析归档
  - `wiki/decisions/` - 技术决策记录
  - `wiki/drafts/` - 草稿
- `manifests/` - 素材清单
- `scripts/` - 处理脚本

## 网站内容目录
- `content/products/` - 网站产品页面（从 wiki/entities/ 同步）
- `content/selection-guide/` - 网站技术文章（从 wiki/topics/ 同步）

## 工作流程

### 1. 每次会话开始
1. 读取 `wiki/_schema.md` 了解架构规范
2. 读取 `wiki/index.md` 了解当前知识库状态
3. 读取 `wiki/log.md` 了解最近操作

### 2. Ingest - 处理新素材
当用户提供新的 PDF 素材时：
1. 读取 PDF 内容
2. 分析并提取所有产品信息和技术主题
3. 在 `wiki/entities/` 下生成产品页面
4. 在 `wiki/topics/` 下生成技术主题页面
5. 使用 `[[PageName]]` 格式建立双向链接
6. 更新 `manifests/raw-sources.csv`
7. 更新 `wiki/index.md`
8. 追加到 `wiki/log.md`
9. （可选）同步到 `content/products/` 和 `content/selection-guide/`

### 3. Query - 查询知识库
当用户提问时：
1. 读取 `wiki/index.md` 定位相关页面
2. 深入阅读相关页面
3. 综合回答，使用 `[[PageName]]` 引用
4. 询问是否保存到 `wiki/explorations/`

### 4. Lint - 检查知识库
当用户说"检查知识库"时：
1. 运行 `node scripts/stale-report.js`
2. 检查孤立页面、断链、矛盾
3. 生成修复建议

### 5. Sync - 同步到网站
当用户要求同步时：
1. 把 `wiki/entities/` 下的页面同步到 `content/products/`
2. 把 `wiki/topics/` 下的页面同步到 `content/selection-guide/`

## 常用命令模板

### 处理新 PDF 并生成 wiki
```
请按照以下步骤处理这个 PDF 素材：

1. 读取 PDF 内容
2. 分析并提取所有产品信息和技术主题
3. 在 llm-wiki-karpathy/wiki/entities/ 下生成产品页面
4. 在 llm-wiki-karpathy/wiki/topics/ 下生成技术主题页面
5. 使用 [[PageName]] 格式建立双向链接
6. 更新 llm-wiki-karpathy/manifests/raw-sources.csv
7. 更新 llm-wiki-karpathy/wiki/index.md
8. 追加到 llm-wiki-karpathy/wiki/log.md
```

### 处理新 PDF 并同步到网站
```
请按照以下步骤处理这个 PDF 素材：

1. 读取 PDF 内容
2. 分析并提取所有产品信息和技术主题
3. 在 llm-wiki-karpathy/wiki/entities/ 下生成产品页面
4. 在 llm-wiki-karpathy/wiki/topics/ 下生成技术主题页面
5. 使用 [[PageName]] 格式建立双向链接
6. 更新 llm-wiki-karpathy/manifests/raw-sources.csv
7. 更新 llm-wiki-karpathy/wiki/index.md
8. 追加到 llm-wiki-karpathy/wiki/log.md
9. 同步内容到 content/products/ 和 content/selection-guide/
```

### 查询知识库
```
请查询知识库，告诉我关于 [产品名称] 的信息。
```

### 检查知识库
```
请检查知识库，运行 lint 检查，看看有没有孤立页面、断链或其他问题。
```

### 同步到网站
```
请把 llm-wiki-karpathy/wiki/entities/ 下的页面同步到 content/products/，把 llm-wiki-karpathy/wiki/topics/ 下的页面同步到 content/selection-guide/。
```

## 代码规范
- 使用 JavaScript 编写脚本
- 所有函数必须有 JSDoc 注释
- 错误处理必须完善
- 日志记录必须详细

## 文件操作
- 读取：使用 `fs/promises`
- 写入：先写临时文件，再重命名
- 备份：重要操作前自动备份

## 注意事项
- 永远不要直接修改 `raw/` 目录中的文件
- 所有 `wiki/` 页面的更新都要记录到 `log.md`
- 使用 `[[双向链接]]` 建立页面关联
- 保持 Frontmatter 的完整性
